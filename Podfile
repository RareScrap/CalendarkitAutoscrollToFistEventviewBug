# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestDayviewWithSegmentedControl' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestDayviewWithSegmentedControl
  pod 'CalendarKit'#, :path => '/Users/rarescrap/Desktop/CustomCalendarKit'

  target 'TestDayviewWithSegmentedControlTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestDayviewWithSegmentedControlUITests' do
    # Pods for testing
  end

end
